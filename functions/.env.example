# Copy to functions/.env and fill in once the Drive sync is set up (see TODO.md).
# Empty / absent = the Drive sync is a safe no-op. These are plain env vars, NOT
# firebase-functions defineString params (those prompt on stdin and hang the
# emulator). functions/.env is gitignored.
DRIVE_SPREADSHEET_ID=
DRIVE_FOLDER_ID=
