{
  "name": "sabeel-institute-time-tracker",
  "version": "0.0.0",
  "private": true,
  "description": "Sabeel Institute Time Tracker — hours tracking for a small nonprofit (Android + web from one Expo codebase, Firebase backend)",
  "workspaces": [
    "packages/*",
    "functions",
    "app"
  ],
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "prepare": "npm run build -w @sabeel/shared",
    "lint": "eslint .",
    "pretypecheck": "npm run build -w @sabeel/shared",
    "typecheck": "npm run typecheck --workspaces --if-present",
    "pretest": "npm run build -w @sabeel/shared",
    "test": "npm run test --workspaces --if-present",
    "test:emulator": "bash scripts/test-emulator.sh",
    "build": "npm run build -w @sabeel/shared && npm run build -w functions"
  },
  "devDependencies": {
    "@eslint/js": "^9.39.0",
    "@types/node": "^22.10.0",
    "eslint": "^9.39.0",
    "globals": "^16.5.0",
    "typescript": "~6.0.3",
    "typescript-eslint": "^8.53.0"
  }
}
